describe('Player', function () {
  var player
  var health, power, defense, x, y, range

  beforeEach(function(){
    health = Math.floor((Math.random() * 10 + 1))
    power = 5
    defense = 5
    x = 500
    y = 100
    range = 300
  })

  describe('Move', function () {
    it('should move to the right with the right arrow', function (){

    })
    it('should move to the left with the left arrow', function () {

    })
    it('should jump with the spacebar', function () {

    })
    it('should bend with the down arrow')
  })
})
